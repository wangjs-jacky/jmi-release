import { middleware_eslint } from "./eslint";
import { middleware_pkgInfo } from "./pkgInfo";
import { middleware_selectNextVersion } from "./selectNextVersion";
import { middleware_changelog } from "./changelog";
import { middleware_gitPush } from "./gitPush";
import { middleware_addTag } from "./addTag";

export {
  middleware_selectNextVersion,
  middleware_eslint,
  middleware_pkgInfo,
  middleware_changelog,
  middleware_gitPush,
  middleware_addTag
}